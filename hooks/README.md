# hooks

Custom React hooks (functions starting with `use...`) that encapsulate
reusable stateful logic — e.g. `useDebounce`, `useMediaQuery`,
`useLocalStorage`.

Nothing lives here yet. Add a hook here when the same piece of stateful
logic is needed in more than one component, rather than before that.
