# Copy this file to ".env.local" and fill in values as you add features.
# Nothing here is used yet — this is just a placeholder for future config
# (e.g. database URL, auth keys, API keys) so the pattern is ready when needed.

NEXT_PUBLIC_APP_NAME=SATHY
